# name: Python package

# on: [push]

# jobs:
#   build:

#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ["3.10", "3.13"]

#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}
#       - name: Install dependencies
#         # This case we are only updating pip, but you could add other dependencies if needed.
#         run: |
#           python -m pip install --upgrade pip
#           # FIXME Is there anything else that needs to be installed?
#           pip install pytest
#           pip install pytest pyyaml
#       - name: Test with pytest
#         run: |
#           pytest




name: tests

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 如果你有 requirements.txt / pyproject.toml，优先用它安装
          # pip install -r requirements.txt
          pip install -U pytest pytest-cov pyyaml

      - name: Run tests with coverage
        run: |
          pytest --cov=. --cov-branch \
                 --cov-report=term-missing \
                 --cov-report=html \
                 --cov-report=xml:coverage.xml

      - name: Upload HTML coverage report
        uses: actions/upload-artifact@v4
        with:
          name: htmlcov-${{ matrix.python-version }}
          path: htmlcov/

      - name: Upload coverage.xml
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml-${{ matrix.python-version }}
          path: coverage.xml
