name: Python package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.13"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        # This case we are only updating pip, but you could add other dependencies if needed.
        run: |
          python -m pip install --upgrade pip
          # FIXME Is there anything else that needs to be installed?
<<<<<<< HEAD
          pip install pytest
=======
>>>>>>> 5c28c01 (Add GitHub Actions CI workflow to run pytest. Answers UCL-COMP0233-25-26/RSE-Classwork#17)
      - name: Test with pytest
        run: |
          pytest
